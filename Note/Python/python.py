# input() <= función que funciona como un promp()
# print()  <= imprime, como 
# int() <= convierte en número, funciona como parseInt()
# str() <= convierte en string 
# and <= y como && en JS
# or <= o como || en JS
# not <= no como !== en JS
# == <= es igual
# != <= distinto de
# float() <= convierte número decimal 
# round() <= round( variable, a cúanto se quiere redondear)

# Métodos de texto
# str.upper() <= cambiar a Mayúsculas
# str.capitalize <= La primera letra en Mayúsculas
# str.strip() <= elimina los espacios basura del principio o el final
# str.lower() <= cambiar a minúscula
# str.replace( 'o', 'a') <= remplaza, ej: o por a
# str[1] <= índice 
# len(str) <= longitud
